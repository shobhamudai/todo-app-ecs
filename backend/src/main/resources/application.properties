# Expose the health endpoint over the web
management.endpoints.web.exposure.include=health

# Show detailed health information, including database connectivity
management.endpoint.health.show-details=always
